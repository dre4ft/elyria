services:
  elyria:
    build: .
    ports:
      - "8000:8000"
      - "8080:8080"  # Catcher proxy
    volumes:
      - ./data:/data
    environment:
      - host=0.0.0.0
      - port=8000
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
